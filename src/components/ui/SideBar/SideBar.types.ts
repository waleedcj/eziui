export type Links = {
    label: string
    href: string
  }
  
  export type SidebarContextProps = {
    open: boolean
    // setOpen: React.Dispatch<React.SetStateAction<boolean>>
    // animate: boolean
  }