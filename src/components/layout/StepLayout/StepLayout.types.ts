export interface StepGuideProps {
    children: React.ReactNode;
    className?: string;
  }
  
  export interface StepSectionProps {
    title: string;
    children: React.ReactNode;
    className?: string;
  }